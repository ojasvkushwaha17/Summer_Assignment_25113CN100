#include<stdio.h>
int main()
{
    int num, product=1;
    printf("Enter a number: ");
    scanf("%d", &num);
    while(num>0)
    {
        product=product*(num%10);
        num=num/10;
    }
    printf("The product of digits is: %d", product);
}
