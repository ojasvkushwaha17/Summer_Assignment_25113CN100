#include<stdio.h>
int main()
{
    int num, count=0;
    printf("enter a binary number: ");
    scanf("%d", &num);
    while(num>0)
    {
        num=num&(num-1);
        count++;
    }
    printf("number of set bits= %d", count);
    return 0;
}
