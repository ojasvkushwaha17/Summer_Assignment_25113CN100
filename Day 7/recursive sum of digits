#include <stdio.h>
int sum(int num)
{
    if(num==0)
    {
        return 0;
    }
    else
    {
        return num%10+sum(num/10);
    }
}
int main()
{
    int num;
    printf("enter the number: ");
    scanf("%d", &num);
    printf("sum of digits= %d\t", sum(num));
    return 0;
}
